{
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:generate": "pnpm run --filter=@repo/api db:generate",
    "db:push": "pnpm run --filter=@repo/api db:push"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.14"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "trippy-root",
  "packageManager": "pnpm@9.7.1"
}
