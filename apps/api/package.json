{
  "name": "@trippy/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "sst shell --stage dev -- bun run --hot server.ts",
    "db:push": "sst shell --stage dev drizzle-kit push",
    "db:generate": "sst shell --stage dev drizzle-kit generate"
  },
  "dependencies": {
    "@clerk/backend": "^1.9.2",
    "@hono/clerk-auth": "^2.0.0",
    "@hono/trpc-server": "^0.3.2",
    "@neondatabase/serverless": "^0.9.4",
    "@trpc/server": "next",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.5.11",
    "superjson": "^2.2.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@trippy/tsconfig": "workspace:*",
    "drizzle-kit": "^0.24.0",
    "esbuild": "^0.23.1",
    "tsx": "^4.17.0",
    "typescript": "~5.3.3",
    "wrangler": "^3.74.0"
  }
}
