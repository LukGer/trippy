{
  "name": "@trippy/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node dist/server.js",
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --outdir=dist",
    "dev": "sst shell --stage dev -- wrangler dev worker.ts",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4",
    "@trpc/server": "next",
    "drizzle-orm": "^0.33.0",
    "superjson": "^2.2.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@trippy/tsconfig": "workspace:*",
    "drizzle-kit": "^0.24.0",
    "esbuild": "^0.23.1",
    "tsx": "^4.17.0",
    "typescript": "~5.3.3",
    "wrangler": "^3.74.0"
  }
}
